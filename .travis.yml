language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t samvidocker/docker-react -f Dockerfile.dev .

scripts:
  - docker run -e CI=true samvidocker/docker-react npm run test